scenario:
  clients: 2
  ramp-up: 2s
  duration: 5s

  host: localhost
  port: 9090

  pretest:
    endpoint: /login
    method: POST
    body:
      username: test@test.com
      password: password

  testloop:
    interval: 1s

    steps:
      - step:
          endpoint: /
          method: GET